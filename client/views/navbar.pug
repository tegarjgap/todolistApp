nav.navbar.navbar-expand-lg.navbar-dark
    a.navbar-brand(href='#')
      img(src="img/clipboards.png", alt="", width="30", height="30")
      |
    a.navbar-brand(href='#')  to-do-list
      |
      button.navbar-toggler(type='button', data-toggle='collapse', data-target='#navbarNavDropdown', aria-controls='navbarNavDropdown', aria-expanded='false', aria-label='Toggle navigation')
        span.navbar-toggler-icon
      |   
      .collapse.navbar-collapse
        ul.navbar-nav.ml-auto
          li.nav-item.active
            a.nav-link(href='#')  
          |       
          li.nav-item
            if !currentUser
              a.btn.btn-warning.pull-md-right(href='signup') sign-up
            if currentUser
               a.nav-item.nav-link(href='profile') My Profile
          if !currentUser
             a.btn.btn-success.pull-md-right(href='login') sign-in
          else
            a.btn.btn-danger.pull-md-right(href='logout') Logout


